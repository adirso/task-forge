# Include inside nginx's http {} context. NGINX worker processes share this zone.
# Set rate to at most the API's LOGIN_RATE_LIMIT_IP per client address.
limit_req_zone $binary_remote_addr zone=taskforge_login_by_ip:10m rate=20r/m;
